# TODO

Only what is active right now. Closed items move to `WORKLOG.md`.

- [ ] Fill `PREFIX` in `~/.config/research-repo/defaults.env` (and `.env`), then `make doctor`.
- [ ] Run `A-0` locally, then on the first reservation.
- [ ] Rewrite `README.md` and `experiments/A/PREMISE.md` with the research question.
- [ ] Paste the Overleaf project URL into `paper/overleaf.toml` and copy `paper/agentmacros.tex` into the project.
