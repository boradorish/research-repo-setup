# A-0: result

Not yet run in this checkout. After `make run EXP=A-0` and the first
`make remote-run`, record here: the run ids, whether `REMOTE_ROOT` is a
persistent mount (from `smoke.json`), and whether GPUs were visible.
